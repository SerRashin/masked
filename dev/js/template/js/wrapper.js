{{banner}}

{{general}}

{{config_source}}


var Masked = (function(doc, win) {
    {{source}}
    return plugin;
})(document, window);
// $.masked = new Masked;

/**
 * Добавить поддержку $(selector).Masked( options );
 */