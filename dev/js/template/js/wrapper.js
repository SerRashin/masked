{{banner}}

{{general}}

{{config_source}}


var Masked = (function(doc) {
    {{source}}
    return plugin;
})(document);
// $.masked = new Masked;

/**
 * Добавить поддержку $(selector).Masked( options );
 */