{{banner}}

{{general}}

{{config_source}}

{{events}}

{{eventBroker}}

var Masked = (function(doc, win) {
  {{source}}
  return plugin;
})(document, window);
// $.masked = new Masked;

/**
 * Добавить поддержку $(selector).Masked( options );
 */